# Redux
install
npm install @reduxjs/toolkit react-redux
 - store :- use to store the current state of the object
 - Action :- all the BL or action of the application
 - dispatcher :- to send the data from store to Action or action to store or with UI
# PWA
- npm install vite-plugin-pwa
- edit vite.config.ts
- npm run build
- npm run preview
install the app in the system 